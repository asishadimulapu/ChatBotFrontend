# Production Environment Overrides (example)
# Copy to .env.production.local for private production settings.

GENERATE_SOURCEMAP=false
FAST_REFRESH=true
WDS_SOCKET_HOST=localhost
CHOKIDAR_USEPOLLING=false
DANGEROUSLY_DISABLE_HOST_CHECK=true
